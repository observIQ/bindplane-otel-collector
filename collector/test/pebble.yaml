receivers:
  filelog:
    include: ["./var/log/syslog.log"]
    storage: pebble

exporters:
  nop:

extensions:
  pebble:
    directory:
      path: /tmp/pebble
    sync: true

service:
  extensions: [pebble]
  pipelines:
    logs:
      receivers: [filelog]
      exporters: [nop]
