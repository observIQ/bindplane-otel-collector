type: restapi

status:
  class: receiver
  stability:
    alpha: [logs, metrics]

tests:
  config:
    url: "https://api.example.com/data"
    auth_mode: "apikey"
    apikey:
      header_name: "X-API-Key"
      value: "test-key"

