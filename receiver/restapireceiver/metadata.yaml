type: restapi

status:
  class: receiver
  stability:
    alpha: [logs, metrics]

tests:
  config:
    url: "https://api.example.com/data"
    auth_mode: "apikey"
    apikey_header_name: "X-API-Key"
    apikey_value: "test-key"

